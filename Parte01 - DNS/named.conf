// This is the primary configuration file for the BIND DNS server named.
//
// Please read /usr/share/doc/bind9/README.Debian.gz for information on the
// structure of BIND configuration files in Debian, *BEFORE* you customize
// this configuration file.
//
// If you are just adding zones, please do that in /etc/bind/named.conf.local

include "/etc/bind/named.conf.options";
include "/etc/bind/named.conf.local";
include "/etc/bind/named.conf.default-zones";
include "/etc/bind/keys/esclavo.key";

logging {
    channel queries_log {
        file "/var/log/bind/queries.log" versions 5 size 5m;
        severity info;
        print-time yes;
    };

    channel transfers_log {
        file "/var/log/bind/transfers.log" versions 5 size 5m;
        severity info;
        print-time yes;
    };

    channel security_log {
        file "/var/log/bind/security.log" versions 5 size 5m;
        severity info;
        print-time yes;
    };

    category queries {
        queries_log;
    };

    category xfer-in {
        transfers_log;
    };

    category xfer-out {
        transfers_log;
    };

    category security {
        security_log;
    };
};
